name: banca_segura
description: Cliente HTTP Seguro con Certificate Pinning para app bancaria móvil.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/certs/